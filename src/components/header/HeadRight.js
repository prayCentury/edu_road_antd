import React, {PureComponent} from 'react';
import {Menu, Icon} from 'antd';
import Link from 'umi/link';

export default class HeadRight extends PureComponent{
  render(){
    return(
      <div style={{color:'yellow', display:'flex',flex:2}}>
        222222
      </div>
    )
  }
}
